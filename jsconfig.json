{
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      "@/*": ["./*"]  // This will point to all files under the src folder
    }
  },
  "include": [
    "src/**/*"  // Make sure to include all files in the src folder
  ]
}
